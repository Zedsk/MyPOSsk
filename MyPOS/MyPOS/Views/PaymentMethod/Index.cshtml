@model IEnumerable<MyPOS.Data.Entity.PAYMENT_METHOD>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create") |
    @Html.ActionLink("Back to Menu", "Management", "Home")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.method)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.method)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPaymentMethod }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPaymentMethod }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPaymentMethod })
        </td>
    </tr>
}

</table>
